import SideMenu from "@/components/custom/SideMenu";
import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div className="my-5">
      <Button className="bg-teal-800 hover:bg-teal-700">Create new +</Button>
    </div>
  );
};

export default page;
